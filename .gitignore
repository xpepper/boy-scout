# Claude project state
.claude/boy-scout-todos.jsonl
.claude/boy-scout-meta.json
# Written by `claude plugin ... --scope local`; holds an absolute path to this
# checkout, so it is per-machine by construction.
.claude/settings.local.json
# Auto-created on first run, and pure defaults here since the only setting is
# a nudge threshold. A project that has tuned it would want the opposite.
.claude/boy-scout-config.json

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/

# OS
.DS_Store
Thumbs.db
